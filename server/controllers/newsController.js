var News = require('../models/newsModel.js');
var testData = require('../test/loadTestData.js');


module.exports = {
  isFakeNews: (req, res, next) {
    // get url off req.params
    // get domain off url
    // check db for domain
      // if domain in db : return "true"
      // else return "false"
  }
}
